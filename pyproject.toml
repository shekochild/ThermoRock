[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "thermorock"
version = "0.1.0"
description = "ThermoRock package"
authors = [{ name = "Your Name" }]
